#include <unistd.h>
/**
 * _putchar - writes the character c to standout
 * @c: the character to print
 * Return: on success 1
 * if error, -1 is returned  and error set appropriately
 */

int _putchar.c(char c)
{
	return (write(, &c, 1));
}
